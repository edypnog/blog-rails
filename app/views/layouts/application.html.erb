<!DOCTYPE html>
<html>
  <head>
    <title>Edy Nogueira | Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

  </head>

  <body>
    <div class="bg-dark text-center text-light mb-4 p-4">

      <%= link_to root_path, class: 'text-light nav-link' do %>
        <h1 class="display-4">Blog de Notícias</h1>
      <% end %>

      <p>
        Notícias sobre os acontecimentos mundiais!
      </p>
      
    </div>
    <div class="container">

      <% flash.each do |type, message| %>
        <div class="alert alert-<%= type == alert ? 'danger' : 'success' %> alert-dismissible fade show" role="alert">
          <%= message %>
          <button type="button" class="btn btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>

      <div class="row">
        <div class="col-4 offset-8">
          <div class="search">
            <%= form_with url: search_posts_path, method: :get, local: true, class: 'form-inline' do |form| %>
              <div class="form-group mx-3 mb-3">
                <%= form.search_field :q, class: 'form-control', required: true, placeholder: 'Buscar' %>
              </div>

              <%= form.submit 'Buscar', class: 'btn btn-secondary mb-3', data: { disable_with: 'Buscando' } %>

            <% end %>
          </div>
        </div>
      </div>

      <%= yield %>
    </div>

    <footer class="p-3 mt-4 text-center bg-dark text-light">
      <p> Copyright &copy Edy Nogueira <%= Date.current.year %> </p>
    </footer>
  </body>
</html>
